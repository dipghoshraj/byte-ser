# byte-ser
Rust light weight trait for converting the types to bytes and deserialize into the data type. Just to escape serde increasing the binary size, if you don't have hard constrain for binary size then there is no point of using this.
